include: macros/patch.def
page: t, patch_frame
    module: perl
    patch: REPLACE
    # root:  $ENV{HOME}/work/pull_requests/2408_coll_group

#---------------------------------------- 
subcode: grep_REPLACE
    $call grep_l, 'mpi_errno_ret'

subcode: filter_lines_REPLACE
    # int mpi_errno_ret ATTRIBUTE((unused)) = MPI_SUCCESS;
    $if $(l)=~/^\s*int mpi_errno_ret ATTRIBUTE\S* = MPI_SUCCESS;/
        $cnt++
        # $(l) =~s/MPIR_ERR_COLL_CHECKANDCONT.*;/MPIR_ERR_CHECK(mpi_errno);/
        $(l) = "-DELETE"

